import React from 'react';

// heading title bar for all pages
const Navbar = () => {
    return(
        <nav className="navbar navbar-dark bg-dark mb-5">
            <span className="navbar-brand mb-0 h1 mx-auto">Total Sounds</span>
        </nav>
    );
}

export default Navbar;